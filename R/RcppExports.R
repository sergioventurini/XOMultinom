# Generated by using Rcpp::compileAttributes() -> do not edit by hand
# Generator token: 10BE3573-1514-4C36-9D1C-5A225CD40393

#' Probability that the maximum multinomial count is at most a threshold
#'
#' Computes \eqn{P(\max_k N_k \le c)} for multinomial cell counts with total
#' count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param c Numeric threshold for the maximum cell count.
#' @return A probability between 0 and 1.
#' @export
prob_max_leq <- function(n, pi, c) {
    .Call(`_XOMultinom_prob_max_leq`, n, pi, c)
}

#' Probability that the minimum multinomial count is at least a threshold
#'
#' Computes \eqn{P(\min_k N_k \ge c)} for multinomial cell counts with total
#' count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param c Numeric threshold for the minimum cell count.
#' @return A probability between 0 and 1.
#' @export
prob_min_geq <- function(n, pi, c) {
    .Call(`_XOMultinom_prob_min_geq`, n, pi, c)
}

#' Probability that the maximum multinomial count exceeds a threshold
#'
#' Computes \eqn{P(\max_k N_k > c)} for multinomial cell counts with total
#' count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param c Numeric threshold for the maximum cell count.
#' @return A probability between 0 and 1.
#' @export
prob_max_gt <- function(n, pi, c) {
    .Call(`_XOMultinom_prob_max_gt`, n, pi, c)
}

#' Probability that the minimum multinomial count is below a threshold
#'
#' Computes \eqn{P(\min_k N_k < c)} for multinomial cell counts with total
#' count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param c Numeric threshold for the minimum cell count.
#' @return A probability between 0 and 1.
#' @export
prob_min_lt <- function(n, pi, c) {
    .Call(`_XOMultinom_prob_min_lt`, n, pi, c)
}

#' Probability that the minimum multinomial count is at most a threshold
#'
#' Computes \eqn{P(\min_k N_k \le c)} for multinomial cell counts with total
#' count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param c Numeric threshold for the minimum cell count.
#' @return A probability between 0 and 1.
#' @export
prob_min_leq <- function(n, pi, c) {
    .Call(`_XOMultinom_prob_min_leq`, n, pi, c)
}

#' Joint probability for maximum and minimum multinomial counts
#'
#' Computes \eqn{P(\max_k N_k \le a, \min_k N_k \ge b)} for multinomial cell
#' counts with total count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param a Numeric upper threshold for the maximum cell count.
#' @param b Numeric lower threshold for the minimum cell count.
#' @return A probability between 0 and 1.
#' @export
prob_joint <- function(n, pi, a, b) {
    .Call(`_XOMultinom_prob_joint`, n, pi, a, b)
}

#' Probability mass for the maximum multinomial count
#'
#' Computes \eqn{P(\max_k N_k = c)} for multinomial cell counts with total
#' count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param c Numeric count value at which to evaluate the probability mass.
#' @return A probability between 0 and 1.
#' @export
prob_max_eq <- function(n, pi, c) {
    .Call(`_XOMultinom_prob_max_eq`, n, pi, c)
}

#' Probability mass for the minimum multinomial count
#'
#' Computes \eqn{P(\min_k N_k = c)} for multinomial cell counts with total
#' count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param c Numeric count value at which to evaluate the probability mass.
#' @return A probability between 0 and 1.
#' @export
prob_min_eq <- function(n, pi, c) {
    .Call(`_XOMultinom_prob_min_eq`, n, pi, c)
}

#' Probability mass vector for maximum multinomial counts
#'
#' Computes \eqn{P(\max_k N_k = c)} for each integer `c` in the inclusive range
#' from `c_lo` to `c_hi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param c_lo Lower bound of the count range.
#' @param c_hi Upper bound of the count range.
#' @return Numeric vector of probability masses for the requested range.
#' @export
pmf_max_range <- function(n, pi, c_lo, c_hi) {
    .Call(`_XOMultinom_pmf_max_range`, n, pi, c_lo, c_hi)
}

#' Probability mass vector for minimum multinomial counts
#'
#' Computes \eqn{P(\min_k N_k = c)} for each integer `c` in the inclusive range
#' from `c_lo` to `c_hi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param c_lo Lower bound of the count range.
#' @param c_hi Upper bound of the count range.
#' @return Numeric vector of probability masses for the requested range.
#' @export
pmf_min_range <- function(n, pi, c_lo, c_hi) {
    .Call(`_XOMultinom_pmf_min_range`, n, pi, c_lo, c_hi)
}

#' Probability that the multinomial count range is below a threshold
#'
#' Computes \eqn{P(\max_k N_k - \min_k N_k < r)} for multinomial cell counts
#' with total count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param r Numeric threshold for the count range.
#' @return A probability between 0 and 1.
#' @export
prob_range_lt <- function(n, pi, r) {
    .Call(`_XOMultinom_prob_range_lt`, n, pi, r)
}

#' Probability that the multinomial count range is at most a threshold
#'
#' Computes \eqn{P(\max_k N_k - \min_k N_k \le r)} for multinomial cell counts
#' with total count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param r Numeric threshold for the count range.
#' @return A probability between 0 and 1.
#' @export
prob_range_leq <- function(n, pi, r) {
    .Call(`_XOMultinom_prob_range_leq`, n, pi, r)
}

#' Probability that the multinomial count range is at least a threshold
#'
#' Computes \eqn{P(\max_k N_k - \min_k N_k \ge r)} for multinomial cell counts
#' with total count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param r Numeric threshold for the count range.
#' @return A probability between 0 and 1.
#' @export
prob_range_geq <- function(n, pi, r) {
    .Call(`_XOMultinom_prob_range_geq`, n, pi, r)
}

#' Probability that the multinomial count range exceeds a threshold
#'
#' Computes \eqn{P(\max_k N_k - \min_k N_k > r)} for multinomial cell counts
#' with total count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param r Numeric threshold for the count range.
#' @return A probability between 0 and 1.
#' @export
prob_range_gt <- function(n, pi, r) {
    .Call(`_XOMultinom_prob_range_gt`, n, pi, r)
}

#' Probability mass for the multinomial count range
#'
#' Computes \eqn{P(\max_k N_k - \min_k N_k = r)} for multinomial cell counts
#' with total count `n` and category probabilities `pi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param r Numeric count-range value at which to evaluate the probability mass.
#' @return A probability between 0 and 1.
#' @export
prob_range_eq <- function(n, pi, r) {
    .Call(`_XOMultinom_prob_range_eq`, n, pi, r)
}

#' Probability mass vector for multinomial count ranges
#'
#' Computes \eqn{P(\max_k N_k - \min_k N_k = r)} for each integer `r` in the
#' inclusive range from `r_lo` to `r_hi`.
#'
#' @param n Integer total count.
#' @param pi Numeric vector of category probabilities.
#' @param r_lo Lower bound of the count-range values.
#' @param r_hi Upper bound of the count-range values.
#' @return Numeric vector of probability masses for the requested range.
#' @export
pmf_range_range <- function(n, pi, r_lo, r_hi) {
    .Call(`_XOMultinom_pmf_range_range`, n, pi, r_lo, r_hi)
}

#' Probability mass function of the maximum of a multinomial random vector
#'
#' Computes the probability mass function for the maximum component of a
#' multinomial random vector with total count `size` and category probabilities
#' `prob`, evaluated at each value in `x`.
#'
#' @param x Numeric vector of values at which to evaluate the probability mass
#'   function.
#' @param size Integer total count of the multinomial distribution.
#' @param prob Numeric vector of category probabilities.
#' @param logd Logical; if `TRUE`, probabilities are returned on the log scale.
#' @param verbose Logical; if `TRUE`, displays a progress bar.
#'
#' @return A numeric vector containing the probabilities, or log-probabilities
#'   when `logd = TRUE`.
#'
#' @export
dmaxmultinom_corrado <- function(x, size, prob, logd, verbose) {
    .Call(`_XOMultinom_dmaxmultinom_corrado`, x, size, prob, logd, verbose)
}

#' Minimum probability for multinomial distribution
#'
#' Computes the probability that the minimum cell count in a multinomial
#' sample is equal to a given value.
#'
#' @param x Numeric vector of values at which to evaluate the probability.
#' @param size Integer, the total number of trials.
#' @param prob Numeric vector of category probabilities.
#' @param logd Logical, if TRUE returns log-probabilities.
#' @param verbose Logical, if TRUE shows a progress bar.
#'
#' @return Numeric vector of probabilities (or log-probabilities if \code{logd = TRUE}).
#'
#' @export
dminmultinom_corrado <- function(x, size, prob, logd, verbose) {
    .Call(`_XOMultinom_dminmultinom_corrado`, x, size, prob, logd, verbose)
}

#' Range probability for multinomial distribution
#'
#' Computes the probability that the range of a multinomial sample
#' (i.e., max(X) - min(X)) is equal to a given value.
#'
#' @param x Numeric vector of range values at which to evaluate the probability.
#' @param size Integer, the total number of trials.
#' @param prob Numeric vector of category probabilities.
#' @param logd Logical, if TRUE returns log-probabilities.
#' @param verbose Logical, if TRUE shows a progress bar.
#'
#' @return Numeric vector of probabilities (or log-probabilities if \code{logd = TRUE}).
#'
#' @export
drangemultinom_corrado <- function(x, size, prob, logd, verbose) {
    .Call(`_XOMultinom_drangemultinom_corrado`, x, size, prob, logd, verbose)
}

#' CDF of the sum of the first \eqn{J} largest order statistics
#'
#' Computes the cumulative distribution function of the sum of the first
#' \eqn{J} largest order statistics for an equiprobable multinomial
#' distribution.
#'
#' @param td Numeric value at which to evaluate the CDF. Internally, this is
#'   converted to \code{floor(td)}.
#' @param n Integer number of balls/trials.
#' @param m Integer number of urns/cells.
#' @param J Integer number of largest order statistics to sum.
#'
#' @return Numeric value giving the CDF probability.
#'
#' @references
#' Bonetti, M., Cirillo, P., Ogay, A. (2019). Computing the exact
#' distributions of some functions of the ordered multinomial counts:
#' maximum, minimum, range and sums of order statistics. Royal Society
#' Open Science, 6, 190198. \doi{10.1098/rsos.190198}
#'
#' @examples
#' highest_order_statistics(6, 10, 5, 2)
#'
#' @export
highest_order_statistics <- function(td, n, m, J) {
    .Call(`_XOMultinom_highest_order_statistics`, td, n, m, J)
}

#' CDF of the maximum multinomial cell count
#'
#' Computes the cumulative distribution function of the largest order
#' statistic, that is, the maximum cell count, for an equiprobable
#' multinomial distribution.
#'
#' @param td Numeric value at which to evaluate the CDF. Internally, this is
#'   converted to \code{floor(td)}.
#' @param n Integer number of balls/trials.
#' @param m Integer number of urns/cells.
#'
#' @return Numeric value giving \eqn{P(N_{(m)} \le t)}, where
#'   \eqn{t = \lfloor td \rfloor}.
#'
#' @references
#' Bonetti, M., Cirillo, P., Ogay, A. (2019). Computing the exact
#' distributions of some functions of the ordered multinomial counts:
#' maximum, minimum, range and sums of order statistics. Royal Society
#' Open Science, 6, 190198. \doi{10.1098/rsos.190198}
#'
#' @examples
#' max_order_statistic(3, 10, 5) # P(N_(m) <= 3; n = 10, m = 5)
#'
#' @export
max_order_statistic <- function(td, n, m) {
    .Call(`_XOMultinom_max_order_statistic`, td, n, m)
}

#' CDF of the maximum for a multinomial distribution
#'
#' Computes the cumulative distribution function of the maximum
#' \eqn{M = \max(X_1, \ldots, X_k)} for a multinomial distribution
#' with arbitrary cell probabilities.
#'
#' @param x Numeric vector of values at which to evaluate the CDF.
#' @param size Integer number of trials.
#' @param prob Numeric vector of category probabilities. Must sum to 1.
#' @param logd Logical; if \code{TRUE}, returns log-probabilities.
#' @param verbose Logical; if \code{TRUE}, displays a progress bar.
#'
#' @return Numeric vector of the same length as \code{x} containing
#'   \eqn{P(M \le x)} (or log-probabilities if \code{logd = TRUE}).
#'
#' @details
#' The function evaluates the probability that the maximum of multinomial
#' cell counts does not exceed the specified values. Computation is
#' performed element-wise over \code{x}.
#'
#' @examples
#' pmaxmultinom_corrado(0:3, size = 10, prob = rep(1/5, 5),
#'                      logd = FALSE, verbose = FALSE)
#'
#' @export
pmaxmultinom_corrado <- function(x, size, prob, logd, verbose) {
    .Call(`_XOMultinom_pmaxmultinom_corrado`, x, size, prob, logd, verbose)
}

#' CDF of the minimum for a multinomial distribution
#'
#' Computes the cumulative distribution function of the minimum
#' \eqn{M = \min(X_1, \ldots, X_k)} for a multinomial distribution
#' with arbitrary cell probabilities.
#'
#' @param x Numeric vector of values at which to evaluate the CDF.
#' @param size Integer number of trials.
#' @param prob Numeric vector of category probabilities. Must sum to 1.
#' @param logd Logical; if \code{TRUE}, returns log-probabilities.
#' @param verbose Logical; if \code{TRUE}, displays a progress bar.
#'
#' @return Numeric vector of the same length as \code{x} containing
#'   \eqn{P(M \le x)} (or log-probabilities if \code{logd = TRUE}).
#'
#' @details
#' The function evaluates the probability that the minimum of multinomial
#' cell counts does not exceed the specified values. Computation is
#' performed element-wise over \code{x}.
#'
#' @examples
#' pminmultinom_corrado(0:3, size = 10, prob = rep(1/5, 5),
#'                      logd = FALSE, verbose = FALSE)
#'
#' @export
pminmultinom_corrado <- function(x, size, prob, logd, verbose) {
    .Call(`_XOMultinom_pminmultinom_corrado`, x, size, prob, logd, verbose)
}

#' CDF of the range for a multinomial distribution
#'
#' Computes the cumulative distribution function of the range
#' \eqn{R = \max(X_1, \ldots, X_k) - \min(X_1, \ldots, X_k)}
#' for a multinomial distribution with arbitrary cell probabilities.
#'
#' @param x Numeric vector of values at which to evaluate the CDF.
#' @param size Integer number of trials.
#' @param prob Numeric vector of category probabilities. Must sum to 1.
#' @param logd Logical; if \code{TRUE}, returns log-probabilities.
#' @param verbose Logical; if \code{TRUE}, displays a progress bar.
#'
#' @return Numeric vector of the same length as \code{x} containing
#'   \eqn{P(R \le x)} (or log-probabilities if \code{logd = TRUE}).
#'
#' @details
#' The function evaluates the probability that the range of multinomial
#' cell counts does not exceed the specified values. Computation is
#' performed element-wise over \code{x}.
#'
#' @examples
#' prangemultinom_corrado(0:3, size = 10, prob = rep(1/5, 5),
#'                        logd = FALSE, verbose = FALSE)
#'
#' @export
prangemultinom_corrado <- function(x, size, prob, logd, verbose) {
    .Call(`_XOMultinom_prangemultinom_corrado`, x, size, prob, logd, verbose)
}

#' CDF of the range of multinomial cell counts
#'
#' Computes the cumulative distribution function of the range, that is,
#' the difference between the maximum and minimum cell counts, for an
#' equiprobable multinomial distribution.
#'
#' @param td Numeric value at which to evaluate the CDF. Internally, this is
#'   converted to \code{floor(td)}.
#' @param n Integer number of balls/trials.
#' @param m Integer number of urns/cells.
#'
#' @return Numeric value giving \eqn{P(N_{(m)} - N_{(1)} \le t)}, where
#'   \eqn{t = \lfloor td \rfloor}.
#'
#' @references
#' Bonetti, M., Cirillo, P., Ogay, A. (2019). Computing the exact
#' distributions of some functions of the ordered multinomial counts:
#' maximum, minimum, range and sums of order statistics. Royal Society
#' Open Science, 6, 190198. \doi{10.1098/rsos.190198}
#'
#' @examples
#' range_probability(1, 7, 10)
#'
#' @export
range_probability <- function(td, n, m) {
    .Call(`_XOMultinom_range_probability`, td, n, m)
}

