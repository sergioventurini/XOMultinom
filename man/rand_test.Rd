% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maxmin_sample_size.R
\name{rand_test}
\alias{rand_test}
\title{Apply a Randomized Test Decision Rule}
\usage{
rand_test(obs_max, kappa, gamma)
}
\arguments{
\item{obs_max}{An integer giving the observed maximum bin count.}

\item{kappa}{An integer threshold defining the rejection region.}

\item{gamma}{A numeric value in \verb{[0, 1]} giving the rejection probability
when \code{obs_max == kappa - 1}.}
}
\value{
A logical or integer indicator of rejection:
\describe{
\item{\code{1L}}{the test rejects deterministically (\code{obs_max >= kappa}).}
\item{\code{0L} or \code{1L}}{a randomized decision when \code{obs_max == kappa - 1}.}
\item{\code{FALSE}}{the test does not reject (\code{obs_max < kappa - 1}).}
}
}
\description{
Implements a randomized decision rule based on an observed maximum bin count.
The test rejects with probability 1 when the observed count is at least
\code{kappa}, rejects with probability \code{gamma} when the observed count equals
\code{kappa - 1}, and does not reject otherwise.
}
\details{
Randomization at the boundary is performed using \code{stats::rbinom()}.
}
