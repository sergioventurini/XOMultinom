% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{prangemultinom_corrado}
\alias{prangemultinom_corrado}
\title{CDF of the range for a multinomial distribution}
\usage{
prangemultinom_corrado(x, size, prob, logd, verbose)
}
\arguments{
\item{x}{Numeric vector of values at which to evaluate the CDF.}

\item{size}{Integer number of trials.}

\item{prob}{Numeric vector of category probabilities. Must sum to 1.}

\item{logd}{Logical; if \code{TRUE}, returns log-probabilities.}

\item{verbose}{Logical; if \code{TRUE}, displays a progress bar.}
}
\value{
Numeric vector of the same length as \code{x} containing
\eqn{P(R \le x)} (or log-probabilities if \code{logd = TRUE}).
}
\description{
Computes the cumulative distribution function of the range
\eqn{R = \max(X_1, \ldots, X_k) - \min(X_1, \ldots, X_k)}
for a multinomial distribution with arbitrary cell probabilities.
}
\details{
The function evaluates the probability that the range of multinomial
cell counts does not exceed the specified values. Computation is
performed element-wise over \code{x}.
}
\examples{
prangemultinom_corrado(0:3, size = 10, prob = rep(1/5, 5),
                       logd = FALSE, verbose = FALSE)

}
